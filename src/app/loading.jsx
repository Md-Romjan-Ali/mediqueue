import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center mt-[30%]'>
            <h1>Loading...</h1>
        </div>
    );
};

export default Loading;